emAIl Sentinel — rebuild prompt
Last updated: 2026-04-25

=== WHAT THIS PROJECT IS ===

emAIl Sentinel (exact branding: lowercase e, lowercase m, uppercase A, uppercase I, lowercase l, space, uppercase S, lowercase entinel) is a production Google Workspace Gmail Add-on built with Google Apps Script (V8 runtime). It watches Gmail labels for new messages, evaluates each one against user-defined plain-English rules using the Google Gemini REST API, and dispatches alerts via SMS, Google Chat, Google Calendar, Google Sheets, Google Tasks, Google Docs, or MCP server endpoints.

Operated by JJJJJ Enterprises, LLC (jjjjj_enterprises_llc@protonmail.com).

=== TECH STACK ===

- Runtime: Google Apps Script (server-side JavaScript, V8 runtime)
- Host: Google's infrastructure — no external backend, no database
- UI: Google Cards Service (Gmail sidebar add-on cards)
- State: PropertiesService.getUserProperties() — per-user, per-script, private, 9 KB per value limit
- AI: Google Gemini REST API (UrlFetchApp to generativelanguage.googleapis.com)
- Auth: OAuth 2.0 scopes declared in appsscript.json; ScriptApp.getOAuthToken() for Google-native channels
- No npm, no build step, no transpilation. Deploy with `clasp push`.

=== FILE STRUCTURE ===

Code.gs           — Entry points: onHomepage(), universal actions, onUninstall()
Cards.gs          — All CardService UI: home card, rules list, rule editor, settings, activity log, help
                    Settings-card helpers: isSmsConfigReady_(s) gates the Send-test-SMS button's
                    disabled state at render; findMcpServerById_(id) supports preserving MCP auth
                    tokens when the field is left blank on edit. handleSaveSettings diffs the
                    submitted form against stored settings and returns "No changes to save." when
                    nothing changed — CardService has no client-side reactivity to build a true
                    dirty-state button, so this toast is the closest UX equivalent
MailWatcher.gs    — Time-driven trigger handler: polls Gmail, diffs against seen-IDs, dispatches matches
RuleEvaluator.gs  — Gemini REST calls: rule evaluation (match?) + alert message formatting
AlertDispatcher.gs — Alert dispatch: BYO SMS (presets + generic webhook), Chat, Calendar, Sheets, Tasks, Docs, MCP
McpServers.gs     — MCP server CRUD + JSON-RPC 2.0 tool dispatch over HTTPS
RulesManager.gs   — CRUD for rules stored in UserProperties
SettingsManager.gs — CRUD for settings; business-hours helpers
ActivityLog.gs    — Ring-buffered activity log with batch-write support (startLogBuffering / flushLog)
LicenseManager.gs — Tier limits (Free vs Pro), feature gating, founding-member counter
appsscript.json   — Manifest: OAuth scopes, add-on descriptor, time-driven trigger config

Help content lives inline in helpTopics_() in Cards.gs (5 topics: quickstart,
examples, channels, pricing, settings). The Help card renders it via
buildHelpCard / handleShowHelpTopic / handleSearchHelp. There is no separate
Help.html file — the card uses CardService text widgets, not HtmlService.

Community support is on GitHub Discussions:
https://github.com/StephenRJohns/email_sentinel/discussions
Reachable from inside the add-on three ways: (1) home-card "Community" nav
button (plain TextButton.setOpenLink, opens FULL_SIZE), (2) kebab "⋮" menu
"Community discussions" universal action (actionOpenDiscussions returns a
UniversalActionResponseBuilder.setOpenLink — supported pattern for an external
URL from a kebab item, no card render), (3) Help card → Settings &
troubleshooting → "Still stuck?" links. The github.com/ prefix is whitelisted
in appsscript.json openLinkUrlPrefixes. Use Discussions for usage Q&A and
rule-recipe sharing, GitHub Issues for bugs/feature requests.

(The public marketing website — including the emAIl Sentinel product landing
page, all three HTML legal pages, sitemap.xml, and robots.txt — lives in a
sibling repo: ../jjjjjenterprises.com/. This email_sentinel repo is now the
add-on source code + the authoritative markdown for the legal documents;
the HTML renderings are generated into jjjjjenterprises.com/emailsentinel/
from the markdown files in this repo's legal/ dir.)
legal/TERMS.md    — Terms of Service
legal/PRIVACY.md  — Privacy Policy
legal/DISCLAIMER.md — Disclaimer (AI accuracy, IP attribution, no-reliance notice)

=== DATA MODEL ===

All state in PropertiesService.getUserProperties():

  mailsentinel.settings — JSON: Gemini key/model, poll interval, business hours, SMS config, alert channel IDs, license tier
  mailsentinel.rules    — JSON array of rule objects (see RulesManager.gs header for schema)
  mailsentinel.seen     — JSON: per-label array of recently-seen Gmail message IDs (dedup baseline)
  mailsentinel.log      — Ring buffer of last ~60 activity log lines

=== LICENSE / PRICING TIERS ===

Free:  3 rules max, automatic polling in 60-min increments (min 180 — every 3 hours), SMS + Calendar + Sheets + Tasks + Docs, AI-assisted alert content, 30-day log, GitHub Issues support
Pro:   Unlimited rules, automatic polling in 60-min increments (min 60 — every 1 hour, the Workspace add-on platform floor), all channels (+ Chat + MCP), AI-assisted rule writing, unlimited log, GitHub Issues support — $4.99/mo or $39/yr
Both:  "Scan email now" button runs an immediate manual check regardless of polling cadence; bypasses the LAST_RUN_KEY skip-check via runMailCheck({force: true}).
Note:  The 60-minute floor is a Google Workspace platform constraint on time-based triggers — cannot be bypassed at the add-on level.
Founding Member: All Pro features for life, one-time $79, limited to first 500 purchasers

Tier stored in settings.license.tier ('free' | 'pro'). Pro entitlement sync with Marketplace Subscription API is planned; currently set manually via setTierPro / setTierFree (no-arg wrappers in LicenseManager.gs around the private setTier_(tier) helper) for testing.

=== ALERT CHANNELS ===

SMS:             BYO provider — six quick-start presets (Textbelt, Telnyx, Plivo, Twilio, ClickSend, Vonage)
                 plus Generic webhook (POST {"to": "+1...", "body": "..."} to any HTTPS endpoint)
Google Chat:     Webhook POST to user-configured Chat Space URLs (Pro only)
Google Calendar: Creates 15-min event via CalendarApp (uses 'calendar' OAuth scope)
Google Sheets:   Appends row via SpreadsheetApp (uses 'spreadsheets' OAuth scope)
Google Tasks:    Creates task via Tasks REST API + ScriptApp.getOAuthToken() (uses 'tasks' scope)
External integrations: Settings section labeled "External integrations" covers two dispatch shapes — (a) MCP servers (JSON-RPC 2.0 tools/call over HTTPS) and (b) direct HTTPS POST of a body template with no JSON-RPC envelope. Type dropdown options, "Custom X" labels for user-provided endpoints and named-vendor labels for everything else: Custom MCP (Cloudflare-Worker-style HTTPS MCP server, default for new entries — Help has a 15-min walkthrough), Custom webhook (HTTPS POST — any endpoint accepting JSON; Slack incoming webhooks, Discord webhooks, n8n / Zapier / Make scenarios, custom internal APIs), Microsoft Teams (Microsoft Graph MCP, send_message tool, OAuth + admin consent friction), Asana (REST API — easier, PAT-based, not strictly MCP — direct POST), Asana (MCP V2 — requires OAuth, asana_create_task tool, hourly token expiry). The constant DIRECT_POST_TYPES = ['asana-rest', 'webhook'] in McpServers.gs is the canonical list of types that bypass the JSON-RPC envelope and route through sendDirectPost_ instead of sendMcpAlert_'s main path. The editor hides the Tool name field for direct-post types (it's unused) and renames "Tool arguments (JSON)" → "Request body (JSON)". Slack was removed as a named MCP type because Slack does not host an MCP server; users now pick Custom webhook. Microsoft 365 was renamed to Microsoft Teams since Teams chat is the actual alert surface. Legacy stored types `slack` and `ms365` are migrated at editor render time (slack→custom, ms365→teams) so existing user configs survive the rename.

=== KEY DESIGN CONSTRAINTS ===

- Apps Script execution limit: 6 min. runMailCheck() guards with MAX_RUN_MS=180000 (3 min), MAX_EVALS_PER_RUN=100
- 9 KB UserProperties limit per key. saveRules() throws if over. saveSeen_() auto-shrinks buffer
- Log buffering: startLogBuffering() + flushLog() batch writes during trigger runs
- Concurrency guard: LockService.getUserLock(0ms timeout) in runMailCheck(); skip if locked
- Cards are fully stateless: read UserProperties fresh on every render
- In-card Home button on the four root cards (Rules, Settings, Activity log, Help): unconditionally prepended via homeButtonSection_(). Wired to handleGoHome which does popToRoot + updateCard(buildHomeCard()). Always-on rather than conditional because Apps Script doesn't expose navigation-stack depth at handler time, so updateCard refreshes (rule toggle, log refresh, settings save) would silently drop a conditional Home button. Mild redundancy with Gmail's back arrow on push paths is accepted. Starter rules card is the one nav target without it — only reached via push from home, back arrow always available.
- Polling grid: Gmail / Workspace add-on time-based triggers must fire >= 60 min apart. enforcePollFloor() snaps pollMinutes up to the nearest multiple of 60 (Free and Pro both have minPollMinutes=60). installTrigger() uses everyHours(targetHours) where targetHours = round(pollMinutes/60). runMailCheck() still applies a LAST_RUN_KEY elapsed-time skip-check for precise cadence when the user picks longer-than-1-hour intervals. runMailCheck({force: true}) bypasses the skip-check for manual "Scan email now" — the way users get sub-hour responsiveness on demand.
- Home polling dropdown auto-saves: the home card "Scan email every" SelectionInput wires setOnChangeAction → handleHomePollChange, which persists the picked value to settings.pollMinutes the moment the user changes the selection (CardService doesn't auto-submit form inputs on navigation; without this the user could pick a value, navigate to Settings, and find the old value still selected). handleHomePollChange returns an empty ActionResponseBuilder so the save is silent — no toast, no card refresh. handleStartMonitoring re-saves the form value as defense-in-depth.
- Action button color conventions (Cards.gs): BRAND_PURPLE_ #581c87 = primary FILLED CTAs (Save, Generate, + New rule, Start scheduled scans); BRAND_PURPLE_LIGHT_ #7c3aed = secondary FILLED CTA when a primary purple sits beside it (Scan email now); BRAND_RED_ #c62828 = every Delete button (rule list Delete + Delete all rules + 3 editor cards + their confirmation cards) with whiteText_ wrapper. The rule toggle reads "Off" when the rule is currently on (action: turn off) and "On" when currently off, plain TEXT style both states. Short labels chosen because CardService scales row widths down at higher card-section counts (e.g. 5+ rules) and longer labels (Disable/Enable) wrapped the Delete button onto a second row at those counts. State is visible from the section header (✅ ON / ⏸ OFF), so the button doesn't duplicate it. A FILLED-yellow Disable was tried earlier but CardService aligns column widths across all sections in a card, so the wider FILLED button on one ON rule inflated every section's toggle column. BRAND_YELLOW_LIGHT_ + blackText_ remain defined but unused. Text color is wrapped via whiteText_ for dark FILLED backgrounds (purple, red).
- Phone number entry: country-code dropdown (~45 countries with flag emoji + dial code, default +1) plus digits-only input. splitPhoneNumber_/combinePhoneNumber_ handle E.164 storage and round-trip on edit.
- AI assist gating: "Help me write the rule text" is Pro-only; "Help me write the alert text" is available on Free. Both buttons open a description-input card; the user types informally, Gemini cleans it into a rule description or alert format. The alert-text flow is channel-aware — it factors in whichever channels are ticked on the rule.
- First-run baseline: first check per label records seen IDs, no alerts (intentional behavior)
- Gemini called via REST (UrlFetchApp), not SDK — only needs script.external_request scope
- Email body: only first 2,000 characters sent to Gemini

=== BRANDING / IP ===

Product name: emAIl Sentinel™ (exact capitalization required everywhere)
Trademark: Pending at USPTO — serial number 99761473, filed 2026-04-23. Use ™ only, not ®, until USPTO issues a registration certificate.
Operator: JJJJJ Enterprises, LLC
Contact routing: legal@ / billing@ / support@ / admin@ / privacy@ @jjjjjenterprises.com
Related products: PilotTrainerHQ (pilottrainerhq.com), PlaneFacts (planefacts.online)
Google, Gmail, Google Workspace, Gemini, Google Chat, Calendar, Sheets, Tasks, Docs are trademarks of Google LLC
SMS provider names are trademarks of their respective owners
emAIl Sentinel is not affiliated with or endorsed by any third-party service provider
Geographic scope: launch is restricted to United States only. EU/UK/EEA/CH availability deferred until demand justifies an EU representative + lawyer review. Marketplace listing must be region-restricted at publish time. Legal docs (legal/TERMS.md §2, legal/PRIVACY.md §9) state US-only explicitly.

=== OAUTH SCOPES (all actively used) ===

gmail.addons.execute      — add-on execution
gmail.readonly            — GmailApp.search() and message reading
calendar                  — CalendarApp in sendCalendarAlert_()
spreadsheets              — SpreadsheetApp in sendSheetsAlert_()
tasks                     — Tasks REST API in sendTasksAlert_()
script.external_request   — UrlFetchApp for Gemini + SMS providers + MCP servers
script.scriptapp          — ScriptApp.newTrigger() / getProjectTriggers()

=== DEPLOY ===

npm install -g @google/clasp
clasp login
clasp push          # push all .gs / .html / appsscript.json to linked Apps Script project
clasp open-script   # open in browser editor for test deployment (clasp v3+ renamed from `clasp open`)

The public marketing/legal HTML lives in the sibling repo ../jjjjjenterprises.com/. After editing the markdown in legal/, regenerate the HTML legal pages with:
  cd ../jjjjjenterprises.com && python3 render_legal.py
